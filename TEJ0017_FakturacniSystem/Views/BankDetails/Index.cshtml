@model IEnumerable<TEJ0017_FakturacniSystem.Models.PaymentMethod.BankDetail>

@{
    ViewData["Title"] = "Index";
}

<ul class="nav nav-tabs">
    <li class="nav-item">
        <a class="nav-link" href="#">Aplikace</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="#">Uživatel</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="#">Firma</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="/PaymentMethods">Platební metody</a>
    </li>
    <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="/BankDetails">Bankovní účty</a>
    </li>
</ul>

<br />
<h1><i class="bi bi-credit-card"></i> Bankovní účty</h1>

<p>
    <a class="btn btn-primary" asp-action="Create"><i class="bi bi-plus"></i>Nový bankovní účet</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BankName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AccountNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BankCode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PaymentMethodId">@Html.DisplayFor(modelItem => item.Name)</a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BankName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AccountNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BankCode)
            </td>
            <td>
                <a asp-action="Delete" asp-route-id="@item.PaymentMethodId" data-bs-toggle="tooltip" data-bs-placement="bottom" title="Odstranění bankovního účtu"><i class="bi bi-trash-fill"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>

<!--Tooltip trigger-->
<script>
    var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
    var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl)
    {
    return new bootstrap.Tooltip(tooltipTriggerEl)
    })
</script>
